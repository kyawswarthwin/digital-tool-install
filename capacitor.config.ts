import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.kyawswar.digitaltoolinstall',
  appName: 'Digital Tool Install',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
